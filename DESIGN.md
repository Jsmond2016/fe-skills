# fe-skills — UI 设计与交互规范

> 本文档规范项目的 UI 设计语言与交互行为，是前端开发与设计协作的依据。
> 由 `fe-set-ai-base` 初始化生成，本项目为 CLI 工具仓库，无 UI 运行时，此文档作为占位。

## 设计系统

### 设计语言

- **风格**: N/A（CLI 工具仓库，无图形界面）
- **品牌色**: N/A
- **字体**: N/A

## 交互模式

### CLI 交互

- 所有管理操作通过 npm scripts 触发
- 交互式命令（如 `create-skill`）使用 readline 提供命令行引导
- 非交互式命令（如 `sync-agent-links`）支持 `--dry-run` 预览模式

### 输出规范

- 成功操作使用 `✓` 标记
- 错误信息使用 `✗` 标记
- 变更信息使用 `~` 标记
- 使用标准颜色：绿色（成功）、红色（错误）、黄色（警告）

---

> **维护说明**: 如后续增加 Web 管理界面，设计团队补充此文档。
